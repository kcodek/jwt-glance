# Git Workflow & Commit Rules

## Never Commit or Push Automatically

- **Do NOT run `git commit` or `git push` autonomously.**
- Stage changes (`git add`) if requested or needed, but **always stop and wait for explicit user review and permission** before committing or pushing changes to any repository.
- Present the staged status, diff summary, or verification results clearly to the user and await their confirmation.
